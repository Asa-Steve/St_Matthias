import { motion } from "framer-motion";
import styled from "styled-components";
const TagLine = styled(motion.h5)`
  font-size: 1rem;
  font-variant: small-caps;
  font-weight: bold;
  color: var(--color-primary);
`;

export default TagLine;
